fun factorial(n) {
  if (n <= 1) {
    return 1;
  } else {
    return n * factorial(n - 1);
  }
}

var numero = 5;
var resultado = factorial(numero,2);
print "El factorial de " + numero + " es: " + resultado; // 120
